# راهنمای اضافه کردن عضو جدید

## روش 1: از طریق رابط کاربری (UI)

### مراحل:
1. وارد سیستم شوید با حساب مدیر (admin)
2. به منوی **"پرسنل"** یا **"Personnel"** بروید
   - می‌توانید از منوی کناری یا آدرس `/personnel` استفاده کنید
3. روی دکمه **"افزودن پرسنل جدید"** یا **"Add New Personnel"** کلیک کنید
4. فرم را پر کنید:
   - **نام**: نام شخص (الزامی)
   - **نام خانوادگی**: نام خانوادگی شخص (الزامی)
   - **شماره تماس**: شماره تلفن (الزامی)
   - **ایمیل**: ایمیل (اختیاری)
   - **نام کاربری**: نام کاربری برای ورود (اختیاری - اگر خالی باشد، از شماره تماس ساخته می‌شود)
   - **رمز عبور**: رمز عبور (اختیاری - اگر خالی باشد، رمز پیش‌فرض `123456` استفاده می‌شود)
   - **نقش**: نقش کاربر (admin, manager, staff, hr, expert)
   - **Telegram ID**: شناسه تلگرام (اختیاری)
   - **وضعیت**: فعال/غیرفعال
5. روی دکمه **"ذخیره"** کلیک کنید

## روش 2: از طریق API

### استفاده از curl:
```bash
curl -X POST http://localhost:2001/api/personnel \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "first_name": "نام",
    "last_name": "نام خانوادگی",
    "phone": "09123456789",
    "username": "username",
    "password": "123456",
    "role": "staff",
    "isActive": true
  }'
```

### استفاده از JavaScript:
```javascript
import { createPersonnel } from './lib/api'

const newUser = {
  first_name: 'نام',
  last_name: 'نام خانوادگی',
  phone: '09123456789',
  username: 'username',
  password: '123456',
  role: 'staff',
  isActive: true
}

await createPersonnel(newUser)
```

## روش 3: از طریق ثبت‌نام (Register)

### مراحل:
1. به صفحه **"ثبت‌نام"** یا `/register` بروید
2. فرم ثبت‌نام را پر کنید:
   - **نام**: نام شخص
   - **شماره تماس**: شماره تلفن
   - **نام کاربری**: نام کاربری (اختیاری)
   - **رمز عبور**: رمز عبور (اختیاری - پیش‌فرض: `123456`)
   - **نقش**: نقش کاربر (پیش‌فرض: `staff`)
3. روی دکمه **"ثبت‌نام"** کلیک کنید

## نکات مهم:

1. **فیلدهای الزامی**: نام و شماره تماس الزامی هستند
2. **نام کاربری**: اگر خالی باشد، به صورت خودکار از شماره تماس ساخته می‌شود
3. **رمز عبور**: اگر خالی باشد، رمز پیش‌فرض `123456` استفاده می‌شود
4. **نقش‌ها**:
   - `admin`: مدیر سیستم
   - `manager`: مدیر
   - `staff`: کارمند
   - `hr`: منابع انسانی
   - `expert`: کارشناس
5. **شماره تماس تکراری**: اگر شماره تماس قبلاً ثبت شده باشد، خطا نمایش داده می‌شود

## دسترسی‌ها:

- **افزودن پرسنل**: فقط مدیران (admin) می‌توانند پرسنل جدید اضافه کنند
- **ویرایش پرسنل**: فقط مدیران می‌توانند اطلاعات پرسنل را ویرایش کنند
- **حذف پرسنل**: فقط مدیران می‌توانند پرسنل را حذف کنند

## عیب‌یابی:

اگر در اضافه کردن عضو جدید مشکل دارید:
1. مطمئن شوید که با حساب مدیر وارد شده‌اید
2. بررسی کنید که فیلدهای الزامی (نام و شماره تماس) پر شده باشند
3. بررسی کنید که شماره تماس تکراری نباشد
4. لاگ‌های backend را بررسی کنید: `npx pm2 logs sales-backend`

