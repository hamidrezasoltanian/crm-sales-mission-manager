# پیشنهاد معماری داشبورد کارمند

## 📋 خلاصه اجرایی

ایجاد یک داشبورد شخصی برای هر کارمند که امکان ثبت ماموریت، ثبت تماس و مشاهده گزارش‌های شخصی را از طریق وب فراهم می‌کند.

---

## 🎯 اهداف

1. **دسترسی آسان**: کارمندان بتوانند بدون نیاز به تلگرام، از طریق وب کارهای خود را انجام دهند
2. **تجربه کاربری بهتر**: رابط کاربری ساده و کاربردی برای کارمندان
3. **گزارش‌دهی شخصی**: هر کارمند فقط گزارش‌های خودش را ببیند
4. **یکپارچگی**: استفاده از همان API و دیتابیس موجود

---

## 🏗️ ساختار پیشنهادی

### 1. مسیرها (Routes)

```
/my-dashboard          → داشبورد شخصی کارمند
/my-dashboard/missions  → ثبت و مدیریت ماموریت‌های من
/my-dashboard/contacts → ثبت و مدیریت تماس‌های من
/my-dashboard/reports  → گزارش‌های شخصی من
```

### 2. ساختار فایل‌ها

```
frontend/app/
├── my-dashboard/
│   ├── page.tsx                    # داشبورد اصلی (Overview)
│   ├── missions/
│   │   ├── page.tsx                # لیست ماموریت‌های من
│   │   └── new/
│   │       └── page.tsx            # ثبت ماموریت جدید
│   ├── contacts/
│   │   ├── page.tsx                # لیست تماس‌های من
│   │   └── new/
│   │       ├── page.tsx            # ثبت تماس استان
│   │       └── tehran/
│   │           └── page.tsx       # ثبت تماس تهران
│   └── reports/
│       └── page.tsx                # گزارش‌های شخصی
└── components/
    ├── MissionForm.tsx             # فرم ثبت ماموریت (استفاده مجدد)
    ├── ContactForm.tsx             # فرم ثبت تماس (جدید)
    ├── EmployeeStats.tsx            # آمار کارمند (جدید)
    └── EmployeeMissionCard.tsx     # کارت ماموریت برای کارمند (جدید)
```

---

## 📱 صفحات و ویژگی‌ها

### صفحه 1: داشبورد شخصی (`/my-dashboard`)

**ویژگی‌ها:**
- نمایش آمار کلی:
  - تعداد ماموریت‌های در انتظار تایید
  - تعداد ماموریت‌های تایید شده این ماه
  - تعداد تماس‌های این ماه
  - هزینه کل ماموریت‌ها این ماه
- نمودارهای ساده:
  - نمودار وضعیت ماموریت‌ها (pie chart)
  - نمودار تماس‌ها بر اساس نوع (bar chart)
- آخرین فعالیت‌ها:
  - 5 ماموریت اخیر
  - 5 تماس اخیر
- دکمه‌های سریع:
  - "ثبت ماموریت جدید"
  - "ثبت تماس استان"
  - "ثبت تماس تهران"
  - "مشاهده گزارش‌ها"

**UI/UX:**
- کارت‌های آماری با رنگ‌بندی
- طراحی ساده و کاربردی
- Responsive برای موبایل

---

### صفحه 2: ثبت ماموریت (`/my-dashboard/missions/new`)

**فرم ثبت ماموریت:**

1. **انتخاب مرکز**
   - جستجو در لیست مراکز
   - فیلتر بر اساس شهر/استان
   - نمایش مراکز تخصیص داده شده به کارمند

2. **اطلاعات ماموریت**
   - تاریخ ماموریت (Persian Date Picker)
   - یادداشت (textarea)
   - لوکیشن اسنپ (اختیاری):
     - دکمه "استفاده از موقعیت فعلی" (Geolocation API)
     - یا ورود دستی آدرس
   - هزینه اسنپ:
     - محاسبه خودکار از API اسنپ
     - یا ورود دستی

3. **کد تخفیف** (اختیاری)
   - جستجو در کدهای تخفیف
   - اعمال خودکار تخفیف

4. **پیش‌نمایش و ثبت**
   - نمایش خلاصه اطلاعات
   - دکمه "ثبت ماموریت"

**ویژگی‌های خاص:**
- استفاده از Geolocation API برای موقعیت‌یابی
- محاسبه خودکار هزینه اسنپ
- اعتبارسنجی فرم
- نمایش خطاها با Toast

---

### صفحه 3: لیست ماموریت‌های من (`/my-dashboard/missions`)

**ویژگی‌ها:**
- نمایش فقط ماموریت‌های کارمند فعلی
- فیلترها:
  - وضعیت (pending, approved, completed, rejected)
  - تاریخ (این ماه، هفته گذشته، همه)
- جستجو در نام مرکز یا یادداشت
- مرتب‌سازی:
  - جدیدترین اول
  - قدیمی‌ترین اول
  - بر اساس وضعیت
- نمایش اطلاعات:
  - نام مرکز
  - تاریخ
  - وضعیت (با badge رنگی)
  - هزینه
  - یادداشت مدیر (اگر وجود دارد)
- عملیات:
  - مشاهده جزئیات
  - ویرایش (فقط برای pending)
  - حذف (فقط برای pending)

---

### صفحه 4: ثبت تماس استان (`/my-dashboard/contacts/new`)

**فرم ثبت تماس:**

1. **انتخاب مرکز**
   - جستجو بر اساس:
     - نام مرکز
     - نام استان
     - نام کارشناس مسئول
   - فیلتر بر اساس استان

2. **اطلاعات تماس**
   - یادداشت تماس (textarea)
   - برچسب (tags):
     - سرنخ (lead)
     - فرصت (opportunity)
     - مشتری (customer)
     - قدیمی (old_customer)
   - تغییر وضعیت مرکز (اگر قبلاً برچسب داشته):
     - نمایش وضعیت فعلی
     - انتخاب وضعیت جدید

3. **ثبت**
   - دکمه "ثبت تماس"

**ویژگی‌های خاص:**
- جستجوی پیشرفته در مراکز
- نمایش تاریخچه تماس‌های قبلی با همان مرکز
- پیشنهاد برچسب بر اساس تاریخچه

---

### صفحه 5: ثبت تماس تهران (`/my-dashboard/contacts/new/tehran`)

**مشابه تماس استان اما:**
- فقط مراکز تهران نمایش داده می‌شوند
- فیلتر بر اساس کارشناس مسئول
- بقیه ویژگی‌ها مشابه

---

### صفحه 6: لیست تماس‌های من (`/my-dashboard/contacts`)

**ویژگی‌ها:**
- نمایش فقط تماس‌های کارمند فعلی
- فیلترها:
  - نوع تماس (استان/تهران)
  - برچسب
  - تاریخ
- جستجو در نام مرکز یا یادداشت
- نمایش اطلاعات:
  - نام مرکز
  - نوع تماس
  - تاریخ
  - برچسب
  - یادداشت
- عملیات:
  - ویرایش
  - حذف

---

### صفحه 7: گزارش‌های شخصی (`/my-dashboard/reports`)

**گزارش‌ها:**

1. **گزارش هفتگی**
   - تعداد ماموریت‌ها
   - تعداد تماس‌ها
   - هزینه کل
   - نمودار فعالیت روزانه

2. **گزارش ماهانه**
   - آمار کامل ماه جاری
   - مقایسه با ماه قبل
   - نمودارها:
     - ماموریت‌ها بر اساس وضعیت
     - تماس‌ها بر اساس نوع
     - هزینه‌ها بر اساس هفته

3. **گزارش سفارشی**
   - انتخاب بازه زمانی
   - انتخاب نوع گزارش
   - دانلود PDF

**ویژگی‌های خاص:**
- استفاده از همان API گزارش‌های موجود
- فیلتر بر اساس `personnelId` کارمند فعلی
- نمایش نمودارهای تعاملی

---

## 🔧 تغییرات مورد نیاز در Backend

### 1. API Endpoints (در صورت نیاز)

```javascript
// در routes/assignments.js
GET /api/assignments/my-assignments
  - فقط ماموریت‌های کارمند فعلی
  - فیلتر بر اساس personnelId از token/session

// در routes/contacts.js  
GET /api/contacts/my-contacts
  - فقط تماس‌های کارمند فعلی
  - فیلتر بر اساس personnelId از token/session

// در routes/reports.js
GET /api/reports/my-reports
  - گزارش‌های شخصی کارمند
  - فیلتر بر اساس personnelId
```

### 2. Middleware برای Authentication

```javascript
// middleware/employeeAuth.js
- بررسی role === 'staff' یا 'manager'
- استخراج userId از session
- اضافه کردن به req.user
```

---

## 🎨 کامپوننت‌های جدید مورد نیاز

### 1. `EmployeeStats.tsx`
```typescript
// نمایش آمار کارمند در داشبورد
interface EmployeeStatsProps {
  userId: number
}
```

### 2. `ContactForm.tsx`
```typescript
// فرم ثبت تماس (استان یا تهران)
interface ContactFormProps {
  contactType: 'province' | 'tehran'
  onSubmit: (data: ContactData) => void
}
```

### 3. `EmployeeMissionCard.tsx`
```typescript
// کارت نمایش ماموریت برای کارمند
interface EmployeeMissionCardProps {
  assignment: Assignment
  onEdit?: () => void
  onDelete?: () => void
}
```

### 4. `CenterSearch.tsx`
```typescript
// جستجو و انتخاب مرکز
interface CenterSearchProps {
  onSelect: (center: Center) => void
  filters?: {
    province?: string
    expert?: string
    type?: 'province' | 'tehran'
  }
}
```

### 5. `LocationPicker.tsx`
```typescript
// انتخاب موقعیت جغرافیایی
interface LocationPickerProps {
  onLocationSelect: (lat: number, lng: number, address: string) => void
  initialLocation?: { lat: number, lng: number }
}
```

---

## 🔐 امنیت و دسترسی

1. **Role-Based Access Control**
   - فقط کارمندان با role `staff` یا `manager` دسترسی دارند
   - هر کارمند فقط داده‌های خودش را می‌بیند

2. **Validation**
   - اعتبارسنجی در Frontend و Backend
   - بررسی `personnelId` در تمام درخواست‌ها

3. **Session Management**
   - استفاده از `sessionStorage` برای ذخیره userId
   - بررسی در هر درخواست API

---

## 📱 Responsive Design

- **Desktop**: Layout کامل با Sidebar
- **Tablet**: Sidebar قابل جمع شدن
- **Mobile**: 
  - Navigation bottom bar
  - فرم‌ها full-screen
  - کارت‌ها stacked

---

## 🚀 نقشه راه پیاده‌سازی

### فاز 1: زیرساخت (1-2 روز)
- [ ] ایجاد مسیر `/my-dashboard`
- [ ] ایجاد کامپوننت `EmployeeDashboardLayout`
- [ ] اضافه کردن لینک در Sidebar
- [ ] Middleware برای دسترسی کارمند

### فاز 2: داشبورد اصلی (2-3 روز)
- [ ] صفحه Overview با آمار
- [ ] کامپوننت `EmployeeStats`
- [ ] نمایش آخرین فعالیت‌ها
- [ ] دکمه‌های سریع

### فاز 3: ثبت ماموریت (3-4 روز)
- [ ] صفحه ثبت ماموریت جدید
- [ ] بهبود `MissionForm` برای استفاده مجدد
- [ ] یکپارچه‌سازی Geolocation API
- [ ] محاسبه هزینه اسنپ
- [ ] اعتبارسنجی و ثبت

### فاز 4: مدیریت ماموریت‌ها (2-3 روز)
- [ ] صفحه لیست ماموریت‌های من
- [ ] فیلترها و جستجو
- [ ] کامپوننت `EmployeeMissionCard`
- [ ] عملیات ویرایش/حذف

### فاز 5: ثبت تماس (3-4 روز)
- [ ] کامپوننت `ContactForm`
- [ ] کامپوننت `CenterSearch`
- [ ] صفحه ثبت تماس استان
- [ ] صفحه ثبت تماس تهران
- [ ] اعتبارسنجی و ثبت

### فاز 6: مدیریت تماس‌ها (2 روز)
- [ ] صفحه لیست تماس‌های من
- [ ] فیلترها و جستجو
- [ ] عملیات ویرایش/حذف

### فاز 7: گزارش‌ها (2-3 روز)
- [ ] صفحه گزارش‌های شخصی
- [ ] فیلتر بر اساس `personnelId`
- [ ] نمودارها و جداول
- [ ] دانلود PDF

### فاز 8: تست و بهبود (2-3 روز)
- [ ] تست تمام قابلیت‌ها
- [ ] بهبود UX
- [ ] Responsive testing
- [ ] Bug fixes

**زمان کل تخمینی: 17-24 روز کاری**

---

## 💡 پیشنهادات بهبود

1. **Push Notifications**
   - اطلاع‌رسانی تایید/رد ماموریت
   - استفاده از Web Push API

2. **Offline Support**
   - ذخیره موقت داده‌ها در IndexedDB
   - همگام‌سازی هنگام اتصال

3. **Dark Mode**
   - تم تاریک برای راحتی چشم

4. **Export Data**
   - خروجی Excel/CSV برای گزارش‌ها

5. **Mobile App**
   - تبدیل به PWA
   - نصب روی موبایل

---

## ❓ سوالات برای تصمیم‌گیری

1. آیا کارمندان باید بتوانند ماموریت‌های تایید شده را ویرایش کنند؟
2. آیا نیاز به تایید تماس‌ها توسط مدیر داریم؟
3. آیا محدودیتی برای تعداد ماموریت/تماس در روز وجود دارد؟
4. آیا نیاز به سیستم اعلان‌رسانی داریم؟
5. آیا می‌خواهیم از همان Sidebar استفاده کنیم یا Sidebar جداگانه برای کارمندان؟

---

## 📝 نتیجه‌گیری

این معماری یک راه‌حل جامع و قابل توسعه برای داشبورد کارمند ارائه می‌دهد که:
- ✅ از زیرساخت موجود استفاده می‌کند
- ✅ تجربه کاربری خوبی دارد
- ✅ قابل توسعه است
- ✅ امنیت را در نظر می‌گیرد
- ✅ Responsive است

آماده شروع پیاده‌سازی هستم! 🚀

