# 🔧 Fix اضافه کردن /status_<id>

## ✅ تغییرات:

### 1. اضافه شدن `/status_<id>`:
- حالا هر دو فرمت کار می‌کنند:
  - `/status_1` ✅ (جدید)
  - `/status 1` ✅ (قبلی)

### 2. بهبود لاگ‌ها:
- لاگ‌های کامل برای همه handlers
- نشان می‌دهد که کدام handler trigger می‌شود

### 3. Refactor کد:
- منطق `/status` به تابع `handleStatusCommand` منتقل شد
- کد تمیزتر و قابل نگهداری‌تر شد

## 📋 دستورات جدید:

```
/status       - راهنما
/status_1     - جزئیات ماموریت شماره 1 (جدید)
/status 1     - جزئیات ماموریت شماره 1 (قبلی)
```

## 🧪 تست:

**لطفاً این دستورات را تست کنید:**
```
1. /start
2. /status
3. /status_1
4. /status 1
5. /missions
6. /help
```

**Backend راه‌اندازی مجدد شد. لطفاً تست کنید!**

