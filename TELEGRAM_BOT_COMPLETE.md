# ✅ ربات تلگرام کامل شد

## 🎉 دستورات اضافه شده:

### دستورات عمومی:
1. **`/start`** - شروع کار با ربات و نمایش منوی اصلی با Inline Keyboard
2. **`/missions`** - لیست ماموریت‌های کاربر
3. **`/newmission`** - ایجاد ماموریت جدید (step-by-step)
4. **`/status <id>`** - نمایش جزئیات کامل ماموریت
5. **`/report`** - گزارش‌های شخصی (آمار و هزینه‌ها)
6. **`/help`** - راهنمای کامل دستورات

### دستورات مدیران:
7. **`/pending`** - لیست ماموریت‌های در انتظار تایید
8. **`/approve_<id>`** - تایید ماموریت (از طریق command یا Inline Keyboard)

## 🔄 فرآیند کار:

### 1️⃣ ایجاد ماموریت (`/newmission`):
- **مرحله 1:** انتخاب مرکز (از لیست Inline Keyboard)
- **مرحله 2:** وارد کردن هزینه اسنپ (می‌توان رد کرد)
- **مرحله 3:** وارد کردن کد تخفیف (اختیاری)
- **مرحله 4:** وارد کردن یادداشت (اختیاری)
- **نهایی:** نمایش خلاصه و ثبت ماموریت

**بعد از ثبت:**
- ماموریت با وضعیت `pending` ایجاد می‌شود
- به تمام مدیران اطلاع داده می‌شود
- به پرسنل نیز اطلاع داده می‌شود

### 2️⃣ تایید ماموریت (مدیران):
- مدیر `/pending` می‌زند
- لیست ماموریت‌های در انتظار را می‌بیند
- با Inline Keyboard می‌تواند تایید یا رد کند
- یا از `/approve_<id>` استفاده کند

**بعد از تایید:**
- به پرسنل اطلاع داده می‌شود
- ماموریت وضعیت `approved` می‌گیرد

### 3️⃣ مشاهده وضعیت (`/status <id>`):
- نمایش جزئیات کامل ماموریت
- برای مدیران: دکمه‌های تایید/رد نمایش داده می‌شود

## 🎨 ویژگی‌های UX:

- ✅ **Inline Keyboard** برای همه دستورات
- ✅ **Markdown formatting** برای پیام‌ها
- ✅ **Step-by-step** برای ایجاد ماموریت
- ✅ **اطلاع‌رسانی خودکار** به مدیران و پرسنل
- ✅ **دسترسی بر اساس نقش** (admin, manager, staff)
- ✅ **پیام‌های خطا** واضح و مفید

## 📋 فلوچارت:

```
کارمند: /newmission
  ↓
انتخاب مرکز
  ↓
وارد کردن هزینه اسنپ (یا رد)
  ↓
وارد کردن کد تخفیف (یا رد)
  ↓
وارد کردن یادداشت (یا رد)
  ↓
ثبت ماموریت
  ↓
🔔 اطلاع به مدیران
  ↓
مدیر: /pending
  ↓
دیدن لیست ماموریت‌ها
  ↓
تایید یا رد
  ↓
🔔 اطلاع به کارمند
  ↓
کارمند: /status <id>
  ↓
مشاهده جزئیات تایید شده
```

## 🚀 آماده استفاده است!

**نکته:** دستور `/snapp` در مرحله بعد پیاده‌سازی می‌شود (پیچیده است).

