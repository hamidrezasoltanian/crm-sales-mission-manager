# 📊 ساختار دیتابیس مشترک - توضیح دقیق

## 🎯 پاسخ به سوال شما

**نه، کل دیتای هر دو اپلیکیشن روی یک دیتابیس نمی‌آید!**

**فقط مراکز و داده‌های مرتبط با مراکز مشترک می‌شوند.**

---

## 📁 ساختار پیشنهادی

### ✅ جداول مشترک (Shared Tables)

این جداول **فقط یک بار** در دیتابیس مشترک وجود دارند و **هر دو اپلیکیشن** از آن‌ها استفاده می‌کنند:

```sql
-- کارمندان (مشترک) ✨
personnel

-- مراکز (مشترک)
centers

-- پرسنل مرتبط با مرکز (مشترک)
center_personnel

-- پست‌های سازمانی (مشترک)
organization_positions
```

**چرا مشترک؟**
- **کارمندان:** برای آنالیز و گزارش‌گیری بهتر، یکپارچگی داده‌ها، Foreign Keys ساده‌تر
- **مراکز:** در هر دو اپلیکیشن استفاده می‌شوند، تغییرات باید فوری در هر دو اپ اعمال شود
- **پرسنل مرکز:** مرتبط با مراکز هستند
- **پست‌های سازمانی:** لیست مرجع مشترک

---

### 🔵 جداول اپلیکیشن مدیریت ماموریت (با prefix `mission_`)

این جداول **فقط** برای اپلیکیشن مدیریت ماموریت هستند:

```sql
-- پرسنل اپلیکیشن مدیریت ماموریت
mission_personnel

-- ماموریت‌ها
mission_assignments

-- تماس‌ها
mission_contacts

-- کدهای تخفیف
mission_discount_codes

-- لاگ‌های audit
mission_audit_logs

-- تاریخچه وضعیت مراکز
mission_center_status_history
```

**چرا جدا؟**
- این داده‌ها فقط مربوط به مدیریت ماموریت هستند
- اپلیکیشن آنالیز فروش نیازی به آن‌ها ندارد
- استقلال هر اپلیکیشن حفظ می‌شود

---

### 🟢 جداول اپلیکیشن آنالیز فروش (با prefix `analysis_`)

این جداول **فقط** برای اپلیکیشن آنالیز فروش هستند:

```sql
-- کاربران اپلیکیشن آنالیز فروش
analysis_users

-- محصولات
analysis_products

-- سفارشات
analysis_orders

-- پیش‌فاکتورها
analysis_proformas

-- Workflow ها
analysis_workflows

-- فعالیت‌ها
analysis_activities

-- تنظیمات
analysis_settings

-- KPI Configs
analysis_kpi_configs

-- Mission Logs
analysis_mission_logs
```

**چرا جدا؟**
- این داده‌ها فقط مربوط به آنالیز فروش هستند
- اپلیکیشن مدیریت ماموریت نیازی به آن‌ها ندارد
- استقلال هر اپلیکیشن حفظ می‌شود

---

## 🔗 ارتباط بین جداول

### Foreign Keys:

```sql
-- مراکز به پرسنل (مشترک)
centers.responsiblePersonnelId → personnel.id

-- پرسنل مرکز به مراکز
center_personnel.centerId → centers.id

-- ماموریت‌ها به مراکز و پرسنل (مشترک)
mission_assignments.centerId → centers.id
mission_assignments.personnelId → personnel.id
mission_assignments.managerId → personnel.id

-- تماس‌ها به مراکز
mission_contacts.centerId → centers.id

-- سفارشات به پرسنل (مشترک)
analysis_orders.assigned_to → personnel.id
analysis_orders.created_by → personnel.id

-- Mission Logs به پرسنل (مشترک)
analysis_mission_logs.employee_id → personnel.id
```

---

## 📊 نمودار ساختار

```
┌─────────────────────────────────────────┐
│      دیتابیس مشترک (shared.db)         │
├─────────────────────────────────────────┤
│                                         │
│  ✅ مشترک:                              │
│  ├── personnel (کارمندان) ✨            │
│  ├── centers                            │
│  ├── center_personnel                   │
│  └── organization_positions            │
│                                         │
│  🔵 اپلیکیشن مدیریت ماموریت:           │
│  ├── mission_assignments                │
│  ├── mission_contacts                   │
│  ├── mission_discount_codes             │
│  └── mission_audit_logs                 │
│                                         │
│  🟢 اپلیکیشن آنالیز فروش:              │
│  ├── analysis_products                  │
│  ├── analysis_orders                    │
│  ├── analysis_proformas                 │
│  ├── analysis_workflows                 │
│  ├── analysis_activities                │
│  ├── analysis_settings                  │
│  ├── analysis_kpi_configs               │
│  └── analysis_mission_logs              │
│                                         │
└─────────────────────────────────────────┘
```

---

## 💡 مثال عملی

### سناریو 1: تغییر نام مرکز
- **کجا تغییر می‌دهید؟** در اپلیکیشن مدیریت ماموریت
- **کجا اعمال می‌شود؟** در جدول `centers` (مشترک)
- **نتیجه:** هر دو اپلیکیشن فوراً تغییر را می‌بینند ✅

### سناریو 2: تغییر اطلاعات کارمند
- **کجا تغییر می‌دهید؟** در هر کدام از اپلیکیشن‌ها
- **کجا اعمال می‌شود؟** در جدول `personnel` (مشترک)
- **نتیجه:** هر دو اپلیکیشن فوراً تغییر را می‌بینند ✅

### سناریو 3: ثبت ماموریت جدید
- **کجا ثبت می‌کنید؟** در اپلیکیشن مدیریت ماموریت
- **کجا ذخیره می‌شود؟** در جدول `mission_assignments` (مخصوص اپلیکیشن مدیریت ماموریت)
- **نتیجه:** فقط اپلیکیشن مدیریت ماموریت می‌بیند ✅

### سناریو 4: ثبت سفارش جدید
- **کجا ثبت می‌کنید؟** در اپلیکیشن آنالیز فروش
- **کجا ذخیره می‌شود؟** در جدول `analysis_orders` (مخصوص اپلیکیشن آنالیز فروش)
- **نتیجه:** فقط اپلیکیشن آنالیز فروش می‌بیند ✅

---

## ✅ خلاصه

| نوع جدول | مثال | کجا استفاده می‌شود |
|---------|------|-------------------|
| **مشترک** | `personnel`, `centers`, `center_personnel` | هر دو اپلیکیشن |
| **مخصوص مدیریت ماموریت** | `mission_assignments`, `mission_contacts` | فقط اپلیکیشن مدیریت ماموریت |
| **مخصوص آنالیز فروش** | `analysis_orders`, `analysis_products` | فقط اپلیکیشن آنالیز فروش |

---

## 🎯 نتیجه‌گیری

**کارمندان (`personnel`) و مراکز (`centers`) و داده‌های مرتبط با آن‌ها مشترک می‌شوند.**

**بقیه جداول با prefix جداگانه هستند و هر اپلیکیشن فقط به جداول خودش دسترسی دارد.**

**مزایای جدول مشترک کارمندان:**
- ✅ آنالیز و گزارش‌گیری راحت‌تر
- ✅ یکپارچگی داده‌ها
- ✅ Foreign Keys ساده‌تر
- ✅ جلوگیری از duplicate data

این رویکرد:
- ✅ یکپارچگی داده‌های مراکز را تضمین می‌کند
- ✅ استقلال هر اپلیکیشن را حفظ می‌کند
- ✅ از تداخل داده‌ها جلوگیری می‌کند
- ✅ توسعه و نگهداری را آسان‌تر می‌کند

