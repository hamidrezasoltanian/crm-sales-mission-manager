# 🧪 تست سریع دستورات

## ✅ دستورات برای تست:

```
1. /start
2. /help
3. /missions
4. /report
5. /status_1
```

## 📋 لیست کامل:

### عمومی:
- `/start` - شروع کار
- `/missions` - لیست ماموریت‌ها
- `/newmission` - ایجاد ماموریت جدید
- `/status` - راهنما
- `/status_1` - جزئیات ماموریت شماره 1
- `/status 1` - همان (با فاصله)
- `/report` - گزارش شخصی
- `/help` - راهنما

### مدیران:
- `/pending` - در انتظار تایید
- `/approve_1` - تایید ماموریت شماره 1

---

**Backend راه‌اندازی مجدد شد.**

**لطفاً `/start` را بزنید و ببینید آیا لاگ `📥 /start HANDLER TRIGGERED` را در terminal می‌بینید یا نه.**

اگر لاگ را می‌بینید، یعنی handler کار می‌کند اما پیام ارسال نمی‌شود.
اگر لاگ را نمی‌بینید، یعنی handler trigger نمی‌شود.

