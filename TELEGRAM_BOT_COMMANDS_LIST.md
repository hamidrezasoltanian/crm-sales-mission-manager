# 📋 لیست کامل دستورات ربات تلگرام

## 🎯 دستورات عمومی (برای همه کاربران)

### 1. `/start` - شروع کار با ربات
- نمایش منوی اصلی
- نمایش لیست دستورات
- ایجاد دکمه‌های دسترسی سریع

**مثال:**
```
/start
```

---

### 2. `/missions` - لیست ماموریت‌های من
- نمایش حداکثر 10 ماموریت اخیر کاربر
- نمایش وضعیت هر ماموریت
- لینک به جزئیات هر ماموریت

**مثال:**
```
/missions
```

---

### 3. `/newmission` - ایجاد ماموریت جدید
- فرآیند step-by-step برای ایجاد ماموریت
- انتخاب چندین مرکز (multi-select)
- جستجو و فیلتر مراکز
- انتخاب نوع مرکز (سرنخ، فرصت، مشتری، مشتری قدیمی)
- ثبت هزینه اسنپ (اختیاری)
- ثبت کد تخفیف (اختیاری)
- ثبت یادداشت (اختیاری)

**مثال:**
```
/newmission
```

**نکته:** اگر ماموریت‌هایی بدون هزینه اسنپ دارید، ابتدا باید آن‌ها را تکمیل کنید.

---

### 4. `/status` - راهنمای دستور status
- نمایش راهنمای استفاده از دستور status

**مثال:**
```
/status
```

---

### 5. `/status <id>` یا `/status_<id>` - نمایش جزئیات ماموریت
- نمایش جزئیات کامل یک ماموریت
- نمایش اطلاعات مرکز، هزینه، وضعیت، و...

**مثال:**
```
/status 1
/status_1
```

---

### 6. `/report` - گزارش شخصی
- نمایش آمار کلی ماموریت‌های کاربر
- نمایش تعداد ماموریت‌های pending، approved، completed
- نمایش مجموع هزینه‌ها

**مثال:**
```
/report
```

---

### 7. `/complete_center` - تکمیل اطلاعات مرکز
- نمایش لیست مراکزی که اطلاعات ناقص دارند (بدون آدرس یا شهر)
- تکمیل آدرس، شهر و منطقه برای مراکز

**مثال:**
```
/complete_center
```

---

### 8. `/help` - راهنمای کامل
- نمایش لیست کامل دستورات
- نمایش دستورات مخصوص مدیران (اگر مدیر باشید)

**مثال:**
```
/help
```

---

## 👨‍💼 دستورات مخصوص مدیران (admin/manager)

### 9. `/add` - افزودن مرکز جدید
- فقط برای مدیران
- فرآیند step-by-step برای افزودن مرکز
- ثبت نام، نوع، آدرس، شهر، منطقه
- تعیین مسئول مرکز (اختیاری)

**مثال:**
```
/add
```

**مراحل:**
1. وارد کردن نام مرکز
2. انتخاب نوع (سرنخ، فرصت، مشتری، مشتری قدیمی)
3. وارد کردن آدرس
4. وارد کردن شهر
5. وارد کردن منطقه (اختیاری)
6. انتخاب مسئول (اختیاری)

---

### 10. `/pending` - لیست ماموریت‌های در انتظار تایید
- نمایش همه ماموریت‌های pending
- نمایش دکمه‌های تایید/رد برای هر ماموریت
- نمایش جزئیات هر ماموریت

**مثال:**
```
/pending
```

---

### 11. `/approve` - راهنمای دستور approve
- نمایش راهنمای استفاده از دستور approve

**مثال:**
```
/approve
```

---

### 12. `/approve_<id>` - تایید ماموریت
- تایید یک ماموریت pending
- ارسال پیام تایید به پرسنل

**مثال:**
```
/approve_1
```

---

## 🔄 Callback Query Handlers (دکمه‌های Inline)

### دکمه‌های منو:
- `menu_missions` - نمایش ماموریت‌ها
- `menu_newmission` - ایجاد ماموریت جدید
- `menu_report` - نمایش گزارش
- `menu_complete_center` - تکمیل اطلاعات مرکز
- `menu_pending` - نمایش ماموریت‌های pending (فقط مدیران)
- `menu_help` - نمایش راهنما

### دکمه‌های ماموریت:
- `status_<id>` - نمایش جزئیات ماموریت
- `approve_<id>` - تایید ماموریت
- `reject_<id>` - رد ماموریت
- `add_snapcost_<id>` - افزودن هزینه اسنپ به ماموریت

### دکمه‌های انتخاب مرکز:
- `toggle_center_<id>` - انتخاب/لغو انتخاب مرکز
- `confirm_centers` - تایید انتخاب مراکز
- `cancel_mission` - لغو ایجاد ماموریت
- `search_centers` - جستجوی مراکز
- `show_all_centers` - نمایش همه مراکز
- `center_list_page_<page>_<search>_<type>_<responsible>` - صفحه‌بندی لیست مراکز

### دکمه‌های فیلتر:
- `filter_type_lead` - فیلتر نوع: سرنخ
- `filter_type_opportunity` - فیلتر نوع: فرصت
- `filter_type_customer` - فیلتر نوع: مشتری
- `filter_type_old_customer` - فیلتر نوع: مشتری قدیمی
- `filter_type_clear` - حذف فیلتر نوع
- `filter_responsible_<id>` - فیلتر بر اساس مسئول (فقط مدیران)
- `filter_responsible_clear` - حذف فیلتر مسئول

### دکمه‌های تکمیل مرکز:
- `complete_center_<id>` - شروع تکمیل اطلاعات مرکز
- `complete_center_page_<page>` - صفحه‌بندی مراکز ناقص
- `skip_district` - رد کردن مرحله منطقه

### دکمه‌های افزودن مرکز:
- `add_center_type_<type>` - انتخاب نوع مرکز
- `add_center_skip_district` - رد کردن مرحله منطقه
- `add_center_responsible_<id>` - انتخاب مسئول مرکز
- `add_center_skip_responsible` - رد کردن مرحله مسئول

### دکمه‌های ماموریت:
- `skip_snapcost` - رد کردن هزینه اسنپ
- `skip_discountcode` - رد کردن کد تخفیف
- `skip_notes` - رد کردن یادداشت
- `finish_mission` - ثبت نهایی ماموریت
- `skip_incomplete_snapcost` - ادامه بدون تکمیل هزینه اسنپ

---

## 📝 Conversation Steps

### برای `/newmission`:
1. `center_selection` - انتخاب مراکز
2. `center_search` - جستجوی مراکز
3. `snapcost` - وارد کردن هزینه اسنپ
4. `discountcode` - وارد کردن کد تخفیف
5. `notes` - وارد کردن یادداشت

### برای `/add`:
1. `add_center_name` - وارد کردن نام
2. `add_center_type` - انتخاب نوع
3. `add_center_address` - وارد کردن آدرس
4. `add_center_city` - وارد کردن شهر
5. `add_center_district` - وارد کردن منطقه
6. `add_center_responsible` - انتخاب مسئول

### برای `/complete_center`:
1. `center_address` - وارد کردن آدرس
2. `center_city` - وارد کردن شهر
3. `center_district` - وارد کردن منطقه
4. `center_finish` - تکمیل

### برای افزودن هزینه اسنپ:
1. `add_snapcost` - وارد کردن هزینه اسنپ

---

## 🔍 ویژگی‌های پیشرفته

### جستجو در مراکز:
- در `/newmission` می‌توانید از دکمه "جستجو" استفاده کنید
- جستجو بر اساس نام، آدرس، شهر و نام مسئول انجام می‌شود

### فیلتر بر اساس نوع:
- سرنخ (lead)
- فرصت (opportunity)
- مشتری (customer)
- مشتری قدیمی (old_customer)

### فیلتر بر اساس مسئول (فقط مدیران):
- مدیران می‌توانند مراکز را بر اساس مسئول فیلتر کنند

### انتخاب چندین مرکز:
- در `/newmission` می‌توانید چندین مرکز را انتخاب کنید
- برای هر مرکز یک ماموریت جداگانه ایجاد می‌شود
- مدیر می‌تواند هر ماموریت را جداگانه تایید/رد کند

---

## ✅ وضعیت فعلی

**همه دستورات در فایل `backend/src/services/telegramBot.js` موجود و فعال هستند!**

- ✅ `/start`
- ✅ `/missions`
- ✅ `/newmission`
- ✅ `/status`
- ✅ `/status <id>`
- ✅ `/status_<id>`
- ✅ `/report`
- ✅ `/add` (مدیران)
- ✅ `/complete_center`
- ✅ `/pending` (مدیران)
- ✅ `/approve`
- ✅ `/approve_<id>` (مدیران)
- ✅ `/help`

---

## 🚀 استفاده

1. **کاربران عادی:** می‌توانند از دستورات عمومی استفاده کنند
2. **مدیران:** دسترسی به همه دستورات + دستورات مخصوص مدیران

---

**تاریخ به‌روزرسانی:** $(date)

